image: mcr.microsoft.com/dotnet/sdk:6.0

stages:
  - deploy

deploy:
  tags:
    - kmdev
  stage: deploy
  script:
    - dotnet pack cqrsCore/cqrsCore.csproj -c Release
    - dotnet nuget add source "${CI_API_V4_URL}/groups/${CI_GROUP_ID}/packages/nuget/index.json" --name gitlab --username gitlab-ci-token --password $CI_JOB_TOKEN --store-password-in-clear-text
    - dotnet nuget push "bin/Release/*.nupkg" --source gitlab
  only:
    - master
  environment: production
